# Configuración de Socket.IO para sincronización en tiempo real
# Cambia VITE_ENABLE_SOCKET a 'false' si el servidor no soporta Socket.IO
VITE_ENABLE_SOCKET=true

# URL del servidor Socket.IO (opcional, por defecto usa el backend)
# VITE_RESERVAS_WS_URL=https://macfer.crepesywaffles.com
